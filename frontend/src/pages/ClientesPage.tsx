import { PagePlaceholder } from './PagePlaceholder'

/**
 * Pagina reservada para o modulo de clientes.
 */
export function ClientesPage() {
  return (
    <PagePlaceholder
      title="Clientes"
      description="Base preparada para cadastro, listagem e acompanhamento das clientes do studio com visual mais profissional."
      hint="A camada de servico do modulo ja existe e a tela esta pronta para evoluir para listagem real e formularios."
    />
  )
}
