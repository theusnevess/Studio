import { PagePlaceholder } from './PagePlaceholder'

/**
 * Pagina reservada para o modulo de clientes.
 */
export function ClientesPage() {
  return (
    <PagePlaceholder
      title="Clientes"
      description="Area reservada para cadastro, listagem e edicao de clientes do studio."
    />
  )
}
