package com.studioflow.backend.entity.enums;

/**
 * Estados principais de um agendamento no StudioFlow.
 */
public enum StatusAgendamento {
    AGENDADO,
    CONFIRMADO,
    CONCLUIDO,
    CANCELADO
}
