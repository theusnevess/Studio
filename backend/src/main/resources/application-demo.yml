spring:
  datasource:
    url: jdbc:h2:file:${STUDIOFLOW_DEMO_DB_PATH:${user.home}/studioflow-demo/studioflow-demo};MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: true

logging:
  level:
    org.springframework.security: INFO
